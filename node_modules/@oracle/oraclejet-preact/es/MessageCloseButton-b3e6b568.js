/* @oracle/oraclejet-preact: 15.1.2 */
import { jsx } from 'preact/jsx-runtime';
import './UNSAFE_IconButton.js';
import { CloseIcon as SvgClose } from './PRIVATE_ThemedIcons/CloseIcon.js';
import './utils/UNSAFE_classNames.js';
import { messageCloseButtonStyles } from './PRIVATE_Message/themes/MessageStyles.css.js';
import { I as IconButton } from './IconButton-d6cc2f62.js';
import { c as classNames } from './classNames-f414daad.js';

/**
 * A Component for rendering the message close button
 */
function MessageCloseButton({ onAction, buttonRenderer, title = 'Close', variant = 'banner' }) {
    const renderedButton = buttonRenderer ? (buttonRenderer(title, onAction, variant)) : (jsx(IconButton, { size: "sm", tooltip: title, variant: "borderless", onAction: onAction, children: jsx(SvgClose, {}) }));
    // TODO: JET-48715 - remove marker classes
    // Right now, oj-message-banner component uses this for unit tests and has no alternate way for
    // testing. This will need to be removed as we work on oj-c-message-banner.
    // TODO:  Refactor to utilize useComponentTheme and multiVariantStyles.
    const markerClassForTests = 'oj-c-messagebanner-close-icon';
    const classes = classNames([
        variant === 'banner' && markerClassForTests,
        messageCloseButtonStyles.base,
        messageCloseButtonStyles[variant]
    ]);
    // Otherwise, render the close icon
    return (jsx("div", { class: classes, "data-oj-message-close-button": "", children: renderedButton }));
}

export { MessageCloseButton as M };
//# sourceMappingURL=MessageCloseButton-b3e6b568.js.map
