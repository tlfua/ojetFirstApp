/* @oracle/oraclejet-preact: 15.1.2 */
import { jsx } from 'preact/jsx-runtime';
import { useRef } from 'preact/hooks';
import { useSize } from './hooks/PRIVATE_useSize.js';
import './hooks/PRIVATE_useResizeObserver.js';

const TrackResizeContainer = ({ children, width, height }) => {
    const content = !isFixedLength(width) || !isFixedLength(height) ? (jsx(TrackResizeOnContainer, { width: width, height: height, children: children })) : (jsx("div", { style: { width, height }, children: children(fixedLengthToNumber(width), fixedLengthToNumber(height)) }));
    return content;
};
const TrackResizeOnContainer = ({ children, width, height }) => {
    const ref = useRef(null);
    const size = useSize(ref, { box: 'border-box' });
    return (jsx("div", { ref: ref, style: { width, height }, children: size && children(size.width, size.height) }));
};
const isFixedLength = (length) => length.endsWith('px') || length === '0';
const fixedLengthToNumber = (length) => length === '0' ? 0 : Number(length.substring(0, length.length - 2));

/**
 * @license
 * Copyright (c) %FIRST_YEAR% %CURRENT_YEAR%, Oracle and/or its affiliates.
 * The Universal Permissive License (UPL), Version 1.0
 * as shown at https://oss.oracle.com/licenses/upl/
 * @ignore
 */

export { TrackResizeContainer };
//# sourceMappingURL=PRIVATE_TrackResizeContainer.js.map
