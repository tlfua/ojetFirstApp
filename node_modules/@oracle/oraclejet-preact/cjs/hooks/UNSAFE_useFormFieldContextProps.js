/* @oracle/oraclejet-preact: 15.1.2 */
'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var hooks = require('preact/hooks');

/**
 * A custom hook to determine props to set FormFieldContext
 */
function useFormFieldContextProps({ hasValue, isDisabled, isFocused, isInputFocused, isLoading, isReadonly, value }) {
    return hooks.useMemo(() => {
        return isReadonly
            ? {
                hasValue,
                isFocused,
                isInputFocused,
                isLoading,
                isReadonly: true
            }
            : isDisabled
                ? {
                    hasValue,
                    isDisabled: true,
                    isLoading
                }
                : {
                    hasValue,
                    isFocused,
                    isInputFocused,
                    isLoading
                };
    }, [hasValue, isDisabled, isFocused, isInputFocused, isLoading, isReadonly, value]);
}

exports.useFormFieldContextProps = useFormFieldContextProps;
//# sourceMappingURL=UNSAFE_useFormFieldContextProps.js.map
