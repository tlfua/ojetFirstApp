/* @oracle/oraclejet-preact: 15.1.2 */
'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var hooks = require('preact/hooks');
var hooks_PRIVATE_useResizeObserver = require('./PRIVATE_useResizeObserver.js');

const useSize = (ref, resizeObserverOptions = { box: 'content-box' }) => {
    const [size, setSize] = hooks.useState();
    hooks_PRIVATE_useResizeObserver.useResizeObserver(ref, (entry) => {
        const boxSize = resizeObserverOptions.box === 'border-box' ? entry.borderBoxSize : entry.contentBoxSize;
        const newWidth = boxSize[0].inlineSize;
        const newHeight = boxSize[0].blockSize;
        setSize({ width: newWidth, height: newHeight });
    }, resizeObserverOptions);
    return size;
};

/**
 * @license
 * Copyright (c) %FIRST_YEAR% %CURRENT_YEAR%, Oracle and/or its affiliates.
 * The Universal Permissive License (UPL), Version 1.0
 * as shown at https://oss.oracle.com/licenses/upl/
 * @ignore
 */

exports.useSize = useSize;
//# sourceMappingURL=PRIVATE_useSize.js.map
