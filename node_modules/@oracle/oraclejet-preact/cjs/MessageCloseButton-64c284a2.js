/* @oracle/oraclejet-preact: 15.1.2 */
'use strict';

var jsxRuntime = require('preact/jsx-runtime');
require('./UNSAFE_IconButton.js');
var PRIVATE_ThemedIcons_CloseIcon = require('./PRIVATE_ThemedIcons/CloseIcon.js');
require('./utils/UNSAFE_classNames.js');
var PRIVATE_Message_themes_MessageStyles_css = require('./PRIVATE_Message/themes/MessageStyles.css.js');
var IconButton = require('./IconButton-bc45151b.js');
var classNames = require('./classNames-d619a292.js');

/**
 * A Component for rendering the message close button
 */
function MessageCloseButton({ onAction, buttonRenderer, title = 'Close', variant = 'banner' }) {
    const renderedButton = buttonRenderer ? (buttonRenderer(title, onAction, variant)) : (jsxRuntime.jsx(IconButton.IconButton, { size: "sm", tooltip: title, variant: "borderless", onAction: onAction, children: jsxRuntime.jsx(PRIVATE_ThemedIcons_CloseIcon.CloseIcon, {}) }));
    // TODO: JET-48715 - remove marker classes
    // Right now, oj-message-banner component uses this for unit tests and has no alternate way for
    // testing. This will need to be removed as we work on oj-c-message-banner.
    // TODO:  Refactor to utilize useComponentTheme and multiVariantStyles.
    const markerClassForTests = 'oj-c-messagebanner-close-icon';
    const classes = classNames.classNames([
        variant === 'banner' && markerClassForTests,
        PRIVATE_Message_themes_MessageStyles_css.messageCloseButtonStyles.base,
        PRIVATE_Message_themes_MessageStyles_css.messageCloseButtonStyles[variant]
    ]);
    // Otherwise, render the close icon
    return (jsxRuntime.jsx("div", { class: classes, "data-oj-message-close-button": "", children: renderedButton }));
}

exports.MessageCloseButton = MessageCloseButton;
//# sourceMappingURL=MessageCloseButton-64c284a2.js.map
