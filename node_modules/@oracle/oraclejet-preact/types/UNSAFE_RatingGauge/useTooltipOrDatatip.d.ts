import { ComponentChild } from 'preact';
import { Threshold } from '../utils/UNSAFE_meterTypes';
type Props = {
    max: number;
    value: number;
    isReadonly?: boolean;
    isDisabled?: boolean;
    tooltip?: string;
    datatip?: string;
    thresholds?: Threshold[];
    ariaDescribedBy?: string;
    width: number;
};
export declare function useTooltipOrDatatip({ max, value, isReadonly, isDisabled, tooltip, datatip, thresholds, ariaDescribedBy, width }: Props): {
    tooltipContent: ComponentChild;
    tooltipProps: Record<string, any>;
};
export {};
