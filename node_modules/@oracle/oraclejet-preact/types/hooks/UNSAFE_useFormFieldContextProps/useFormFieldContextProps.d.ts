import { FormFieldContextProps } from '../UNSAFE_useFormFieldContext';
type UseTextFieldProps<V> = {
    hasValue?: boolean;
    isDisabled?: boolean;
    isFocused?: boolean;
    isInputFocused?: boolean;
    isLoading?: boolean;
    isReadonly?: boolean;
    value?: V;
};
/**
 * A custom hook to determine props to set FormFieldContext
 */
export declare function useFormFieldContextProps<V>({ hasValue, isDisabled, isFocused, isInputFocused, isLoading, isReadonly, value }: UseTextFieldProps<V>): FormFieldContextProps;
export {};
