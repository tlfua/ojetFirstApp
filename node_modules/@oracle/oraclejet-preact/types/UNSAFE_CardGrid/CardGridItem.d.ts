/**
 * @license
 * Copyright (c) %FIRST_YEAR% %CURRENT_YEAR%, Oracle and/or its affiliates.
 * The Universal Permissive License (UPL), Version 1.0
 * as shown at https://oss.oracle.com/licenses/upl/
 * @ignore
 */
import { ComponentChildren } from 'preact';
import { StateUpdater } from 'preact/hooks';
import { SelectionMode } from '../UNSAFE_Collection';
export declare const ITEM_STYLE_CLASS = "oj-cardgrid-item";
export declare const ITEM_SELECTOR: string;
/**
 * Props for the CardGridItem Component
 */
export type Props<K, D> = {
    children: ComponentChildren;
    itemKey: K;
    itemIndex: number;
    itemData: D;
    isTabbable: boolean;
    isFocused: boolean;
    isFocusRingVisible: boolean;
    isSelected: boolean;
    selectionMode: SelectionMode;
    initialAnimation?: 'slideUp' | 'slideDown';
    setCardDimension?: StateUpdater<{
        width: number;
        height: number;
    }>;
};
/**
 * The internal component used to render a single item in CardGrid.
 */
export declare function CardGridItem<K extends string | number, D>({ children, itemKey, itemIndex, isTabbable, isFocused, isFocusRingVisible, isSelected, selectionMode, initialAnimation, setCardDimension }: Props<K, D>): import("preact").JSX.Element;
export declare const MemoizeCardGridItem: typeof CardGridItem;
