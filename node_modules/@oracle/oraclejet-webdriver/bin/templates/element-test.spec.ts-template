/**
 * This is a generated test template for @NAME@.  This test should verify the
 * API exposed for the WebElement.
 */
import { expect, assert } from 'chai';
import { By, WebDriver} from 'selenium-webdriver';
import { DriverManager as dm } from '@oracle/oraclejet-webdriver';
import { @CAMELNAME@ } from './index';

describe('Test the WebElement for @NAME@', function() {
  let driver: WebDriver;

  before(async function() {
    driver = await dm.getDriver()
    // TODO: Auto-generated.  Ensure the test URL is correct for this test
    await driver.get('http://localhost/element-test.html');
  })

  it('can verify @NAME@', async function() {
    // TODO: Auto-generated.  Exercise and verify WebElement functionality
    const cwe = await @CAMELNAME@(driver, By.css('@NAME@'));
    await cwe.click();
  })

  after(async function() {
    await dm.releaseDriver(driver);
  })
})
